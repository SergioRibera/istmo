// GENERATED by istmo-build - DO NOT EDIT.
// plugin id: istmo.live_activity

import Foundation

public enum ActivityStyle: UInt32 {
    case standard = 0
    case transient = 1
}

public enum AlertSound {
    case `default`
    case named(String)
    case none
}

public struct AlertConfig {
    public let title: String
    public let body: String
    public let sound: AlertSound

    public init(title: String, body: String, sound: AlertSound) {
        self.title = title
        self.body = body
        self.sound = sound
    }
}

public enum DismissalPolicy {
    case immediate
    case `default`
    case afterSeconds(UInt32)
}

public enum AndroidTierHint: UInt32 {
    case forceCustom = 0
    case preferSystemTemplates = 1
    case requireLiveUpdate = 2
}

public struct IosCapabilities {
    public let activity_kit_available: Bool
    public let activities_enabled: Bool
    public let dynamic_island: Bool
    public let push_updates: Bool

    public init(activity_kit_available: Bool, activities_enabled: Bool, dynamic_island: Bool, push_updates: Bool) {
        self.activity_kit_available = activity_kit_available
        self.activities_enabled = activities_enabled
        self.dynamic_island = dynamic_island
        self.push_updates = push_updates
    }
}

public struct AndroidCapabilities {
    public let supports_custom: Bool
    public let supports_progress_style: Bool
    public let supports_live_update: Bool
    public let notifications_enabled: Bool

    public init(supports_custom: Bool, supports_progress_style: Bool, supports_live_update: Bool, notifications_enabled: Bool) {
        self.supports_custom = supports_custom
        self.supports_progress_style = supports_progress_style
        self.supports_live_update = supports_live_update
        self.notifications_enabled = notifications_enabled
    }
}

public enum PlatformCapabilities {
    case ios(IosCapabilities)
    case android(AndroidCapabilities)
    case unsupported
}

public struct RestoredActivity {
    public let handle: NativeHandleId
    public let activity_type: String
    public let attributes: Data
    public let state: Data

    public init(handle: NativeHandleId, activity_type: String, attributes: Data, state: Data) {
        self.handle = handle
        self.activity_type = activity_type
        self.attributes = attributes
        self.state = state
    }
}

public enum ActivityError: Error {
    case notSupported
    case disabled
    case exceededMaximum
    case handleNotFound
    case unknownActivityType(String)
    case decode(String)
    case backend(String)
}
