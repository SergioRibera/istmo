<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="${istmoLabel}"
        android:icon="${istmoIcon}"
        android:hasCode="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <!-- GameActivity (androidx.games:games-activity) extends
             AppCompatActivity, i.e. a FragmentActivity — which
             BiometricPrompt requires to host its dialog fragment.
             `NativeActivity` cannot show BiometricPrompt. -->
        <activity
            android:name="dev.istmo.biometricdemo.BiometricDemoActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="orientation|keyboardHidden|screenSize">
            <meta-data
                android:name="android.app.lib_name"
                android:value="biometric_demo" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
