<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:hasCode="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <!-- GameActivity (androidx.games:games-activity) so
             `dispatchTouchEvent` runs on the Java side — the Kotlin
             `PenDemoActivity` intercepts stylus samples and forwards
             them into `PenCaptureView` before delegating to super for
             winit / eframe (in `android_main`). `NativeActivity` was
             unusable because it takes the raw `InputQueue`, bypassing
             the view hierarchy. -->
        <activity
            android:name="dev.istmo.pendemo.PenDemoActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="orientation|keyboardHidden|screenSize">
            <meta-data
                android:name="android.app.lib_name"
                android:value="pen_demo" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
