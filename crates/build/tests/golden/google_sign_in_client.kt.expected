// GENERATED by istmo-build - DO NOT EDIT.
// plugin id: istmo.google_sign_in

import java.io.ByteArrayOutputStream

/// Rust-hosted `SignIn` plugin. Ships each call across the frame
/// protocol via `IstmoRuntime.call(...)`.
class SignInClient(
    private val codecs: SignInCodecs,
) {

    companion object {
        const val PLUGIN_ID = "istmo.google_sign_in"
    }

    suspend fun sign_in(mode: SignInMode): SignInAccount {
        val payloadBuf = ByteArrayOutputStream()
        codecs.writeSignInMode(payloadBuf, mode)
        val payload = payloadBuf.toByteArray()
        val bytes = try {
            IstmoRuntime.call(PLUGIN_ID, "sign_in", payload)
        } catch (e: PluginException) {
            val decoded = runCatching { codecs.readSignInError(e.payload, 0).value }.getOrNull()
            throw SignInException(e.payload, decoded)
        }
        var cursor = 0
        val d_result = codecs.readSignInAccount(bytes, cursor); cursor = d_result.consumed
        val result = d_result.value
        return result
    }

    suspend fun silent_sign_in(): SignInAccount? {
        val payload = ByteArray(0)
        val bytes = try {
            IstmoRuntime.call(PLUGIN_ID, "silent_sign_in", payload)
        } catch (e: PluginException) {
            val decoded = runCatching { codecs.readSignInError(e.payload, 0).value }.getOrNull()
            throw SignInException(e.payload, decoded)
        }
        var cursor = 0
        val d_result = Bincode.readOption(bytes, cursor) { p, o -> codecs.readSignInAccount(p, o) }; cursor = d_result.consumed
        val result = d_result.value
        return result
    }

    suspend fun refresh(credential: NativeHandleId): SignInAccount {
        val payloadBuf = ByteArrayOutputStream()
        codecs.writeNativeHandleId(payloadBuf, credential)
        val payload = payloadBuf.toByteArray()
        val bytes = try {
            IstmoRuntime.call(PLUGIN_ID, "refresh", payload)
        } catch (e: PluginException) {
            val decoded = runCatching { codecs.readSignInError(e.payload, 0).value }.getOrNull()
            throw SignInException(e.payload, decoded)
        }
        var cursor = 0
        val d_result = codecs.readSignInAccount(bytes, cursor); cursor = d_result.consumed
        val result = d_result.value
        return result
    }

    suspend fun sign_out() {
        val payload = ByteArray(0)
        try {
            IstmoRuntime.call(PLUGIN_ID, "sign_out", payload)
        } catch (e: PluginException) {
            val decoded = runCatching { codecs.readSignInError(e.payload, 0).value }.getOrNull()
            throw SignInException(e.payload, decoded)
        }
    }

    suspend fun revoke() {
        val payload = ByteArray(0)
        try {
            IstmoRuntime.call(PLUGIN_ID, "revoke", payload)
        } catch (e: PluginException) {
            val decoded = runCatching { codecs.readSignInError(e.payload, 0).value }.getOrNull()
            throw SignInException(e.payload, decoded)
        }
    }
}

/// Kotlin-side view of a domain error decoded from `Frame::Respond { Err(bytes) }`.
class SignInException(
    val payload: ByteArray,
    val decoded: SignInError?,
) : RuntimeException()
