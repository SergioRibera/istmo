# GENERATED by istmo-build — DO NOT EDIT.
# Include this file from your app's `project.yml`:
#   include:
#     - path: RustMobileDemo/istmo-plugins.yml
# xcodegen merges the entries below into your app target; values set
# in project.yml take precedence over the ones here.
targets:
  RustMobileDemo:
    settings:
      base:
        ISTMO_LIB_NAME: "rust_mobile_demo"
        LIBRARY_SEARCH_PATHS:
          - "$(inherited)"
          - "$(PROJECT_DIR)/build/rust/$(PLATFORM_NAME)/$(CURRENT_ARCH)"
        OTHER_LDFLAGS:
          - "$(inherited)"
          - "-lrust_mobile_demo"
        ENABLE_USER_SCRIPT_SANDBOXING: "NO"
        MARKETING_VERSION: "0.1.1"
        CURRENT_PROJECT_VERSION: "1"
        ISTMO_DISPLAY_NAME: "rust-mobile-demo"
        IPHONEOS_DEPLOYMENT_TARGET: "14.0"
    preBuildScripts:
      - name: istmo cargo build
        basedOnDependencyAnalysis: false
        script: '"${PROJECT_DIR}/.istmo/build-rust.sh"'
