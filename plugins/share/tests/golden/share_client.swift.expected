// GENERATED by istmo-build - DO NOT EDIT.
// plugin id: istmo.share

import Foundation

/// Rust-hosted `Share` plugin. Ships each call across the frame
/// protocol via `IstmoRuntime.shared.call(...)`.
public final class ShareClient: Share {
    public static let PLUGIN_ID: String = "istmo.share"

    public init() {}

    public func share(request: ShareRequest) async throws -> ShareOutcome {
        var payload = Data()
        Bincode.encode(&payload, request)
        let bytes = try await IstmoRuntime.shared.call(pluginId: Self.PLUGIN_ID, method: "share", payload: payload)
        return try Bincode.decode(bytes) as ShareOutcome
    }

    public func capabilities() async throws -> ShareCapabilities {
        let payload = Data()
        let bytes = try await IstmoRuntime.shared.call(pluginId: Self.PLUGIN_ID, method: "capabilities", payload: payload)
        return try Bincode.decode(bytes) as ShareCapabilities
    }

    public func staging_dir() async throws -> String {
        let payload = Data()
        let bytes = try await IstmoRuntime.shared.call(pluginId: Self.PLUGIN_ID, method: "staging_dir", payload: payload)
        return try Bincode.decode(bytes) as String
    }

    public func set_share_targets(targets: [ShareTarget]) async throws -> Void {
        var payload = Data()
        Bincode.encode(&payload, targets)
        let bytes = try await IstmoRuntime.shared.call(pluginId: Self.PLUGIN_ID, method: "set_share_targets", payload: payload)
        return try Bincode.decode(bytes) as Void
    }
}

/// Swift-side view of a domain error decoded from `Frame::Respond { Err(bytes) }`.
public struct ShareException: Error {
    public let payload: Data
    public let decoded: ShareError?
}
