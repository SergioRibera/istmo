<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:hasCode="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <!-- Regular AppCompatActivity. `PenDrawingView` is the content
             view (Kotlin renders strokes); `IstmoRuntime.start()`
             loads the Rust .so and starts the JNI pump so the plugin
             wire path is live too. -->
        <activity
            android:name="dev.istmo.pendemo.PenDemoActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="orientation|keyboardHidden|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
