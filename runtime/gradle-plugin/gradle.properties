android.useAndroidX=true
kotlin.code.style=official
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8

# Coordinates published to GitHub Packages Maven registry
# (https://maven.pkg.github.com/SergioRibera/istmo). Independent from
# `runtime/android/gradle.properties` — the plugin loader iterates on
# its own cadence; the `publish-runtime` workflow bumps this value
# separately from `runtimeVersion`.
pluginGroup=dev.istmo
pluginArtifact=istmo-plugin-loader
pluginVersion=0.1.0
