// GENERATED by istmo-build - DO NOT EDIT.
// plugin id: istmo.share
import java.io.ByteArrayOutputStream

class ShareCodecsImpl : ShareCodecs {

    override fun readShareRect(bytes: ByteArray, offset: Int): Bincode.Decoded<ShareRect> {
        var cursor = offset
        val d_x = Bincode.readF64(bytes, cursor)
        cursor = d_x.consumed
        val x = d_x.value
        val d_y = Bincode.readF64(bytes, cursor)
        cursor = d_y.consumed
        val y = d_y.value
        val d_width = Bincode.readF64(bytes, cursor)
        cursor = d_width.consumed
        val width = d_width.value
        val d_height = Bincode.readF64(bytes, cursor)
        cursor = d_height.consumed
        val height = d_height.value
        return Bincode.Decoded(ShareRect(x = x, y = y, width = width, height = height), cursor)
    }

    override fun writeShareRect(out: ByteArrayOutputStream, value: ShareRect) {
        Bincode.writeF64(out, value.x)
        Bincode.writeF64(out, value.y)
        Bincode.writeF64(out, value.width)
        Bincode.writeF64(out, value.height)
    }

    override fun readShareAnchor(bytes: ByteArray, offset: Int): Bincode.Decoded<ShareAnchor> {
        var cursor = offset
        val d_windowId = Bincode.readOption(bytes, cursor) { p, o -> Bincode.readVarintU64(p, o).let { Bincode.Decoded(it.value.toULong(), it.consumed) } }
        cursor = d_windowId.consumed
        val windowId = d_windowId.value
        val d_rect = Bincode.readOption(bytes, cursor) { p, o -> readShareRect(p, o) }
        cursor = d_rect.consumed
        val rect = d_rect.value
        return Bincode.Decoded(ShareAnchor(windowId = windowId, rect = rect), cursor)
    }

    override fun writeShareAnchor(out: ByteArrayOutputStream, value: ShareAnchor) {
        Bincode.writeOption(out, value.windowId) { s, v -> Bincode.writeVarintU64(s, v.toLong()) }
        Bincode.writeOption(out, value.rect) { s, v -> writeShareRect(s, v) }
    }

    override fun readShareFileSource(bytes: ByteArray, offset: Int): Bincode.Decoded<ShareFileSource> {
        var cursor = offset
        val disc = Bincode.readVarintU64(bytes, cursor)
        cursor = disc.consumed
        return when (disc.value.toInt()) {
            0 -> {
                val d_inner = Bincode.readString(bytes, cursor)
                cursor = d_inner.consumed
                val inner = d_inner.value
                Bincode.Decoded(ShareFileSource.Path(inner) as ShareFileSource, cursor)
            }
            1 -> {
                val d_inner = Bincode.readBytes(bytes, cursor)
                cursor = d_inner.consumed
                val inner = d_inner.value
                Bincode.Decoded(ShareFileSource.Bytes(inner) as ShareFileSource, cursor)
            }
            else -> error("ShareFileSource: unknown discriminant ${disc.value}")
        }
    }

    override fun writeShareFileSource(out: ByteArrayOutputStream, value: ShareFileSource) {
        when (value) {
            is ShareFileSource.Path -> {
                Bincode.writeEnumDiscriminant(out, 0)
                Bincode.writeString(out, value.value)
            }
            is ShareFileSource.Bytes -> {
                Bincode.writeEnumDiscriminant(out, 1)
                Bincode.writeBytes(out, value.value)
            }
        }
    }

    override fun readShareFile(bytes: ByteArray, offset: Int): Bincode.Decoded<ShareFile> {
        var cursor = offset
        val d_source = readShareFileSource(bytes, cursor)
        cursor = d_source.consumed
        val source = d_source.value
        val d_name = Bincode.readOption(bytes, cursor) { p, o -> Bincode.readString(p, o) }
        cursor = d_name.consumed
        val name = d_name.value
        val d_mimeType = Bincode.readOption(bytes, cursor) { p, o -> Bincode.readString(p, o) }
        cursor = d_mimeType.consumed
        val mimeType = d_mimeType.value
        return Bincode.Decoded(ShareFile(source = source, name = name, mimeType = mimeType), cursor)
    }

    override fun writeShareFile(out: ByteArrayOutputStream, value: ShareFile) {
        writeShareFileSource(out, value.source)
        Bincode.writeOption(out, value.name) { s, v -> Bincode.writeString(s, v) }
        Bincode.writeOption(out, value.mimeType) { s, v -> Bincode.writeString(s, v) }
    }

    override fun readSharePreview(bytes: ByteArray, offset: Int): Bincode.Decoded<SharePreview> {
        var cursor = offset
        val d_title = Bincode.readOption(bytes, cursor) { p, o -> Bincode.readString(p, o) }
        cursor = d_title.consumed
        val title = d_title.value
        val d_thumbnail = Bincode.readOption(bytes, cursor) { p, o -> readShareFile(p, o) }
        cursor = d_thumbnail.consumed
        val thumbnail = d_thumbnail.value
        val d_fetchLinkMetadata = Bincode.readBool(bytes, cursor)
        cursor = d_fetchLinkMetadata.consumed
        val fetchLinkMetadata = d_fetchLinkMetadata.value
        return Bincode.Decoded(SharePreview(title = title, thumbnail = thumbnail, fetchLinkMetadata = fetchLinkMetadata), cursor)
    }

    override fun writeSharePreview(out: ByteArrayOutputStream, value: SharePreview) {
        Bincode.writeOption(out, value.title) { s, v -> Bincode.writeString(s, v) }
        Bincode.writeOption(out, value.thumbnail) { s, v -> writeShareFile(s, v) }
        Bincode.writeBool(out, value.fetchLinkMetadata)
    }

    override fun readShareRequest(bytes: ByteArray, offset: Int): Bincode.Decoded<ShareRequest> {
        var cursor = offset
        val d_text = Bincode.readOption(bytes, cursor) { p, o -> Bincode.readString(p, o) }
        cursor = d_text.consumed
        val text = d_text.value
        val d_url = Bincode.readOption(bytes, cursor) { p, o -> Bincode.readString(p, o) }
        cursor = d_url.consumed
        val url = d_url.value
        val d_subject = Bincode.readOption(bytes, cursor) { p, o -> Bincode.readString(p, o) }
        cursor = d_subject.consumed
        val subject = d_subject.value
        val d_files = Bincode.readVec(bytes, cursor) { p, o -> readShareFile(p, o) }
        cursor = d_files.consumed
        val files = d_files.value
        val d_preview = Bincode.readOption(bytes, cursor) { p, o -> readSharePreview(p, o) }
        cursor = d_preview.consumed
        val preview = d_preview.value
        val d_anchor = Bincode.readOption(bytes, cursor) { p, o -> readShareAnchor(p, o) }
        cursor = d_anchor.consumed
        val anchor = d_anchor.value
        return Bincode.Decoded(ShareRequest(text = text, url = url, subject = subject, files = files, preview = preview, anchor = anchor), cursor)
    }

    override fun writeShareRequest(out: ByteArrayOutputStream, value: ShareRequest) {
        Bincode.writeOption(out, value.text) { s, v -> Bincode.writeString(s, v) }
        Bincode.writeOption(out, value.url) { s, v -> Bincode.writeString(s, v) }
        Bincode.writeOption(out, value.subject) { s, v -> Bincode.writeString(s, v) }
        Bincode.writeVec(out, value.files) { s, v -> writeShareFile(s, v) }
        Bincode.writeOption(out, value.preview) { s, v -> writeSharePreview(s, v) }
        Bincode.writeOption(out, value.anchor) { s, v -> writeShareAnchor(s, v) }
    }

    override fun readShareOutcome(bytes: ByteArray, offset: Int): Bincode.Decoded<ShareOutcome> {
        var cursor = offset
        val disc = Bincode.readVarintU64(bytes, cursor)
        cursor = disc.consumed
        return when (disc.value.toInt()) {
            0 -> {
                val d_inner = Bincode.readOption(bytes, cursor) { p, o -> Bincode.readString(p, o) }
                cursor = d_inner.consumed
                val inner = d_inner.value
                Bincode.Decoded(ShareOutcome.Shared(inner) as ShareOutcome, cursor)
            }
            1 -> {
                val d_inner = Bincode.readString(bytes, cursor)
                cursor = d_inner.consumed
                val inner = d_inner.value
                Bincode.Decoded(ShareOutcome.TargetChosen(inner) as ShareOutcome, cursor)
            }
            2 -> Bincode.Decoded(ShareOutcome.Dismissed, cursor)
            3 -> Bincode.Decoded(ShareOutcome.Unknown, cursor)
            else -> error("ShareOutcome: unknown discriminant ${disc.value}")
        }
    }

    override fun writeShareOutcome(out: ByteArrayOutputStream, value: ShareOutcome) {
        when (value) {
            is ShareOutcome.Shared -> {
                Bincode.writeEnumDiscriminant(out, 0)
                Bincode.writeOption(out, value.value) { s, v -> Bincode.writeString(s, v) }
            }
            is ShareOutcome.TargetChosen -> {
                Bincode.writeEnumDiscriminant(out, 1)
                Bincode.writeString(out, value.value)
            }
            ShareOutcome.Dismissed -> Bincode.writeEnumDiscriminant(out, 2)
            ShareOutcome.Unknown -> Bincode.writeEnumDiscriminant(out, 3)
        }
    }

    override fun readShareCapabilities(bytes: ByteArray, offset: Int): Bincode.Decoded<ShareCapabilities> {
        var cursor = offset
        val d_send = Bincode.readBool(bytes, cursor)
        cursor = d_send.consumed
        val send = d_send.value
        val d_files = Bincode.readBool(bytes, cursor)
        cursor = d_files.consumed
        val files = d_files.value
        val d_mixedContent = Bincode.readBool(bytes, cursor)
        cursor = d_mixedContent.consumed
        val mixedContent = d_mixedContent.value
        val d_richPreview = Bincode.readBool(bytes, cursor)
        cursor = d_richPreview.consumed
        val richPreview = d_richPreview.value
        val d_reportsCompletion = Bincode.readBool(bytes, cursor)
        cursor = d_reportsCompletion.consumed
        val reportsCompletion = d_reportsCompletion.value
        val d_reportsTarget = Bincode.readBool(bytes, cursor)
        cursor = d_reportsTarget.consumed
        val reportsTarget = d_reportsTarget.value
        val d_receive = Bincode.readBool(bytes, cursor)
        cursor = d_receive.consumed
        val receive = d_receive.value
        return Bincode.Decoded(ShareCapabilities(send = send, files = files, mixedContent = mixedContent, richPreview = richPreview, reportsCompletion = reportsCompletion, reportsTarget = reportsTarget, receive = receive), cursor)
    }

    override fun writeShareCapabilities(out: ByteArrayOutputStream, value: ShareCapabilities) {
        Bincode.writeBool(out, value.send)
        Bincode.writeBool(out, value.files)
        Bincode.writeBool(out, value.mixedContent)
        Bincode.writeBool(out, value.richPreview)
        Bincode.writeBool(out, value.reportsCompletion)
        Bincode.writeBool(out, value.reportsTarget)
        Bincode.writeBool(out, value.receive)
    }

    override fun readShareError(bytes: ByteArray, offset: Int): Bincode.Decoded<ShareError> {
        var cursor = offset
        val disc = Bincode.readVarintU64(bytes, cursor)
        cursor = disc.consumed
        return when (disc.value.toInt()) {
            0 -> {
                val d_inner = Bincode.readString(bytes, cursor)
                cursor = d_inner.consumed
                val inner = d_inner.value
                Bincode.Decoded(ShareError.Unsupported(inner) as ShareError, cursor)
            }
            1 -> {
                val d_inner = Bincode.readString(bytes, cursor)
                cursor = d_inner.consumed
                val inner = d_inner.value
                Bincode.Decoded(ShareError.InvalidRequest(inner) as ShareError, cursor)
            }
            2 -> {
                val d_inner = Bincode.readString(bytes, cursor)
                cursor = d_inner.consumed
                val inner = d_inner.value
                Bincode.Decoded(ShareError.NotFound(inner) as ShareError, cursor)
            }
            3 -> {
                val d_inner = Bincode.readString(bytes, cursor)
                cursor = d_inner.consumed
                val inner = d_inner.value
                Bincode.Decoded(ShareError.NoPresenter(inner) as ShareError, cursor)
            }
            4 -> Bincode.Decoded(ShareError.Busy, cursor)
            5 -> {
                val d_inner = Bincode.readString(bytes, cursor)
                cursor = d_inner.consumed
                val inner = d_inner.value
                Bincode.Decoded(ShareError.Io(inner) as ShareError, cursor)
            }
            6 -> {
                val d_inner = Bincode.readString(bytes, cursor)
                cursor = d_inner.consumed
                val inner = d_inner.value
                Bincode.Decoded(ShareError.Backend(inner) as ShareError, cursor)
            }
            else -> error("ShareError: unknown discriminant ${disc.value}")
        }
    }

    override fun writeShareError(out: ByteArrayOutputStream, value: ShareError) {
        when (value) {
            is ShareError.Unsupported -> {
                Bincode.writeEnumDiscriminant(out, 0)
                Bincode.writeString(out, value.value)
            }
            is ShareError.InvalidRequest -> {
                Bincode.writeEnumDiscriminant(out, 1)
                Bincode.writeString(out, value.value)
            }
            is ShareError.NotFound -> {
                Bincode.writeEnumDiscriminant(out, 2)
                Bincode.writeString(out, value.value)
            }
            is ShareError.NoPresenter -> {
                Bincode.writeEnumDiscriminant(out, 3)
                Bincode.writeString(out, value.value)
            }
            ShareError.Busy -> Bincode.writeEnumDiscriminant(out, 4)
            is ShareError.Io -> {
                Bincode.writeEnumDiscriminant(out, 5)
                Bincode.writeString(out, value.value)
            }
            is ShareError.Backend -> {
                Bincode.writeEnumDiscriminant(out, 6)
                Bincode.writeString(out, value.value)
            }
        }
    }

    override fun readIncomingFile(bytes: ByteArray, offset: Int): Bincode.Decoded<IncomingFile> {
        var cursor = offset
        val d_path = Bincode.readString(bytes, cursor)
        cursor = d_path.consumed
        val path = d_path.value
        val d_name = Bincode.readString(bytes, cursor)
        cursor = d_name.consumed
        val name = d_name.value
        val d_mimeType = Bincode.readOption(bytes, cursor) { p, o -> Bincode.readString(p, o) }
        cursor = d_mimeType.consumed
        val mimeType = d_mimeType.value
        val d_size = Bincode.readOption(bytes, cursor) { p, o -> Bincode.readVarintU64(p, o).let { Bincode.Decoded(it.value.toULong(), it.consumed) } }
        cursor = d_size.consumed
        val size = d_size.value
        return Bincode.Decoded(IncomingFile(path = path, name = name, mimeType = mimeType, size = size), cursor)
    }

    override fun writeIncomingFile(out: ByteArrayOutputStream, value: IncomingFile) {
        Bincode.writeString(out, value.path)
        Bincode.writeString(out, value.name)
        Bincode.writeOption(out, value.mimeType) { s, v -> Bincode.writeString(s, v) }
        Bincode.writeOption(out, value.size) { s, v -> Bincode.writeVarintU64(s, v.toLong()) }
    }

    override fun readIncomingShare(bytes: ByteArray, offset: Int): Bincode.Decoded<IncomingShare> {
        var cursor = offset
        val d_text = Bincode.readOption(bytes, cursor) { p, o -> Bincode.readString(p, o) }
        cursor = d_text.consumed
        val text = d_text.value
        val d_url = Bincode.readOption(bytes, cursor) { p, o -> Bincode.readString(p, o) }
        cursor = d_url.consumed
        val url = d_url.value
        val d_subject = Bincode.readOption(bytes, cursor) { p, o -> Bincode.readString(p, o) }
        cursor = d_subject.consumed
        val subject = d_subject.value
        val d_files = Bincode.readVec(bytes, cursor) { p, o -> readIncomingFile(p, o) }
        cursor = d_files.consumed
        val files = d_files.value
        val d_sourceApp = Bincode.readOption(bytes, cursor) { p, o -> Bincode.readString(p, o) }
        cursor = d_sourceApp.consumed
        val sourceApp = d_sourceApp.value
        val d_receivedAtMs = Bincode.readOption(bytes, cursor) { p, o -> Bincode.readVarintU64(p, o).let { Bincode.Decoded(it.value.toULong(), it.consumed) } }
        cursor = d_receivedAtMs.consumed
        val receivedAtMs = d_receivedAtMs.value
        return Bincode.Decoded(IncomingShare(text = text, url = url, subject = subject, files = files, sourceApp = sourceApp, receivedAtMs = receivedAtMs), cursor)
    }

    override fun writeIncomingShare(out: ByteArrayOutputStream, value: IncomingShare) {
        Bincode.writeOption(out, value.text) { s, v -> Bincode.writeString(s, v) }
        Bincode.writeOption(out, value.url) { s, v -> Bincode.writeString(s, v) }
        Bincode.writeOption(out, value.subject) { s, v -> Bincode.writeString(s, v) }
        Bincode.writeVec(out, value.files) { s, v -> writeIncomingFile(s, v) }
        Bincode.writeOption(out, value.sourceApp) { s, v -> Bincode.writeString(s, v) }
        Bincode.writeOption(out, value.receivedAtMs) { s, v -> Bincode.writeVarintU64(s, v.toLong()) }
    }
}
