{/* GENERATED by istmo-build. DO NOT EDIT. */}
{/* Regenerate with `just docs-types` (or run the `docs-types` bin). */}

### `ShareRect`

Rectangle in the anchor window's coordinate space, in points
(logical pixels), origin top-left.

| Field | Wire | Type | Description |
| ----- | ---- | ---- | ----------- |
| `x` | `x` | `f64` | — |
| `y` | `y` | `f64` | — |
| `width` | `width` | `f64` | — |
| `height` | `height` | `f64` | — |

### `ShareAnchor`

Where the share sheet should appear.

Required on iPad (the sheet is a popover and crashes without an
anchor — the iOS backend falls back to the centre of the key window)
and on macOS (the picker is a popover). Windows uses only the
window. Android ignores it.

| Field | Wire | Type | Description |
| ----- | ---- | ---- | ----------- |
| `window_id` | `windowId` | `Option<u64>` | Desktop: id the window was registered under in [`istmo_window::WindowRegistry`]. `None` picks the lowest registered id. |
| `rect` | `rect` | `Option<ShareRect>` | Element the sheet points at (e.g. the share button). `None` centres the sheet in the window. |

### `ShareFileSource`

Where a [`ShareFile`]'s bytes come from.

| Variant | Payload | Description |
| ------- | ------- | ----------- |
| `Path` | `String` | Absolute path to an existing file readable by the app. On Android the file must live under the app's internal files or cache directory (the `FileProvider` roots); other paths are copied into the cache first. |
| `Bytes` | `Vec<u8>` | In-memory contents. The backend writes them to a temporary file named after [`ShareFile::name`] before sharing. Large payloads cross the wire once — prefer [`Self::Path`] for big files. |

### `ShareFile`

One file attached to a [`ShareRequest`].

| Field | Wire | Type | Description |
| ----- | ---- | ---- | ----------- |
| `source` | `source` | `ShareFileSource` | — |
| `name` | `name` | `Option<String>` | File name shown to the receiving app, e.g. `"report.pdf"`. Required for [`ShareFileSource::Bytes`]; defaults to the path's file name otherwise. |
| `mime_type` | `mimeType` | `Option<String>` | MIME type, e.g. `"application/pdf"`. Guessed from the name when absent. |

### `SharePreview`

Rich preview shown at the top of the share sheet. Opt-in: leave
[`ShareRequest::preview`] as `None` for the platform default.

Honoured by iOS (`LPLinkMetadata`), Android 10+ (`EXTRA_TITLE` and
a `ClipData` thumbnail) and Windows (`DataPackage` title and
thumbnail). macOS ignores it — `NSSharingServicePicker` has no
preview header. Check [`ShareCapabilities::rich_preview`].

| Field | Wire | Type | Description |
| ----- | ---- | ---- | ----------- |
| `title` | `title` | `Option<String>` | Title shown in the preview header. |
| `thumbnail` | `thumbnail` | `Option<ShareFile>` | Image shown in the preview header (PNG/JPEG). |
| `fetch_link_metadata` | `fetchLinkMetadata` | `bool` | iOS only: fetch title and icon from [`ShareRequest::url`] with `LPMetadataProvider` before presenting. Adds a network round-trip; ignored when `url` is `None`. |

### `ShareRequest`

What to share. Any combination of fields may be set; at least one
of `text`, `url` or `files` must be.

| Field | Wire | Type | Description |
| ----- | ---- | ---- | ----------- |
| `text` | `text` | `Option<String>` | Plain text body. |
| `url` | `url` | `Option<String>` | Link. Kept separate from `text` so targets that understand links (messaging apps, browsers) receive a real URL; platforms without a dedicated link slot append it to the text. |
| `subject` | `subject` | `Option<String>` | Subject line for targets that have one (mail). Also the default preview / chooser title. |
| `files` | `files` | `Vec<ShareFile>` | Files to attach. Several files — and files together with text — are supported everywhere sending is. |
| `preview` | `preview` | `Option<SharePreview>` | Optional rich preview; `None` keeps the platform default. |
| `anchor` | `anchor` | `Option<ShareAnchor>` | Where to present the sheet (iPad / macOS / desktop window). |

### `ShareOutcome`

What the user did with the share sheet, as far as the platform
reports it.

| Variant | Payload | Description |
| ------- | ------- | ----------- |
| `Shared` | `Option<String>` | The target reported success. Carries the target id when known (iOS/macOS activity type, Windows app user model id). |
| `TargetChosen` | `String` | The user picked a target but the platform does not report whether the share completed (Android). Carries the target's component / package name. |
| `Dismissed` | — | The user closed the sheet without sharing. |
| `Unknown` | — | The platform gives no feedback (Android when the chooser is dismissed, older Windows builds). |

### `ShareCapabilities`

Which parts of the API the current platform supports.

| Field | Wire | Type | Description |
| ----- | ---- | ---- | ----------- |
| `send` | `send` | `bool` | Any sending at all. `false` on Linux. |
| `files` | `files` | `bool` | Files in [`ShareRequest::files`]. |
| `mixed_content` | `mixedContent` | `bool` | Text and files in the same request. |
| `rich_preview` | `richPreview` | `bool` | [`SharePreview`] is honoured. |
| `reports_completion` | `reportsCompletion` | `bool` | [`ShareOutcome::Shared`] / [`ShareOutcome::Dismissed`] can be reported. |
| `reports_target` | `reportsTarget` | `bool` | The chosen target is reported. |
| `receive` | `receive` | `bool` | The app can be a share destination on this platform (subject to packaging: Share Extension on Apple, MSIX on Windows). |

### `ShareError`

Errors surfaced by [`Share`] methods.

| Variant | Payload | Description |
| ------- | ------- | ----------- |
| `Unsupported` | `String` | The platform (or this build of it) cannot share at all, or not this kind of content. |
| `InvalidRequest` | `String` | The request itself is invalid (empty, unnamed bytes, bad URL). |
| `NotFound` | `String` | A [`ShareFileSource::Path`] does not exist. |
| `NoPresenter` | `String` | No window / view controller / activity to present from. |
| `Busy` | — | Another share sheet is already open. |
| `Io` | `String` | Reading or staging a file failed. |
| `Backend` | `String` | Any other platform failure; message is the raw platform error. |

### `IncomingFile`

A file shared into the app, already copied into app-owned storage.

| Field | Wire | Type | Description |
| ----- | ---- | ---- | ----------- |
| `path` | `path` | `String` | Absolute path of the app-owned copy. |
| `name` | `name` | `String` | Original display name. |
| `mime_type` | `mimeType` | `Option<String>` | — |
| `size` | `size` | `Option<u64>` | — |

### `IncomingShare`

Content another app shared into this one.

| Field | Wire | Type | Description |
| ----- | ---- | ---- | ----------- |
| `text` | `text` | `Option<String>` | — |
| `url` | `url` | `Option<String>` | — |
| `subject` | `subject` | `Option<String>` | — |
| `files` | `files` | `Vec<IncomingFile>` | — |
| `source_app` | `sourceApp` | `Option<String>` | Sending app when the platform reveals it (Android referrer, Windows package family name). |
| `received_at_ms` | `receivedAtMs` | `Option<u64>` | Receive time, milliseconds since the Unix epoch. |
