// GENERATED by istmo-build - DO NOT EDIT.
// plugin id: istmo.admob

import java.io.ByteArrayOutputStream

/// Platform-facing surface for the `AdMob` plugin. Hand-write an
/// impl with the SDK-specific logic; the dispatcher below handles
/// wire encode/decode.
interface AdMobBackend {
     // Throw BackendException<AdError> for domain errors.
    suspend fun load_interstitial(ad_unit_id: String): NativeHandleId

     // Throw BackendException<AdError> for domain errors.
    suspend fun show_interstitial(ad: NativeHandleId): InterstitialOutcome

     // Throw BackendException<AdError> for domain errors.
    suspend fun load_rewarded(ad_unit_id: String): NativeHandleId

     // Throw BackendException<AdError> for domain errors.
    suspend fun show_rewarded(ad: NativeHandleId): RewardedOutcome

     // Throw BackendException<AdError> for domain errors.
    suspend fun show_banner(request: BannerRequest): NativeHandleId

     // Throw BackendException<AdError> for domain errors.
    suspend fun update_banner(banner: NativeHandleId, rect: BannerRect): Unit

     // Throw BackendException<AdError> for domain errors.
    suspend fun hide_banner(banner: NativeHandleId): Unit
}

/// Reader / writer per `Named` type referenced by the `AdMob` contract.
/// User provides one impl; each entry typically delegates to a
/// companion helper on the type itself.
interface AdMobCodecs {
    fun readAdError(bytes: ByteArray, offset: Int): Bincode.Decoded<AdError>
    fun writeAdError(out: ByteArrayOutputStream, value: AdError)

    fun readAdMobConfig(bytes: ByteArray, offset: Int): Bincode.Decoded<AdMobConfig>
    fun writeAdMobConfig(out: ByteArrayOutputStream, value: AdMobConfig)

    fun readBannerRect(bytes: ByteArray, offset: Int): Bincode.Decoded<BannerRect>
    fun writeBannerRect(out: ByteArrayOutputStream, value: BannerRect)

    fun readBannerRequest(bytes: ByteArray, offset: Int): Bincode.Decoded<BannerRequest>
    fun writeBannerRequest(out: ByteArrayOutputStream, value: BannerRequest)

    fun readInterstitialOutcome(bytes: ByteArray, offset: Int): Bincode.Decoded<InterstitialOutcome>
    fun writeInterstitialOutcome(out: ByteArrayOutputStream, value: InterstitialOutcome)

    fun readNativeHandleId(bytes: ByteArray, offset: Int): Bincode.Decoded<NativeHandleId>
    fun writeNativeHandleId(out: ByteArrayOutputStream, value: NativeHandleId)

    fun readRewardedOutcome(bytes: ByteArray, offset: Int): Bincode.Decoded<RewardedOutcome>
    fun writeRewardedOutcome(out: ByteArrayOutputStream, value: RewardedOutcome)
}

/// Decodes wire calls, invokes `AdMobBackend`, encodes the response.
/// Register with `IstmoRuntime.registerHandler(PLUGIN_ID, dispatcher)`.
interface AdMobFactory {
    suspend fun create(config: AdMobConfig): AdMobBackend
}

class AdMobDispatcher(
    private val factory: AdMobFactory,
    private val codecs: AdMobCodecs,
) : PluginHandler, HandleReleaser {

    companion object {
        const val PLUGIN_ID = "istmo.admob"
    }

    private val instances = java.util.concurrent.ConcurrentHashMap<Long, AdMobBackend>()
    private val nextInstanceId = java.util.concurrent.atomic.AtomicLong(1)

    override fun releaseNativeHandle(handleId: Long) {
        for (backend in instances.values) {
            (backend as? HandleReleaser)?.releaseNativeHandle(handleId)
        }
    }

    override suspend fun handleCreateInstance(payload: ByteArray): ByteArray {
        var cursor = 0
        val d_config = codecs.readAdMobConfig(payload, cursor); cursor = d_config.consumed
        val config = d_config.value
        val backend = factory.create(config)
        val id = nextInstanceId.getAndIncrement()
        instances[id] = backend
        val out = ByteArrayOutputStream()
        Bincode.writeVarintU64(out, id)
        return out.toByteArray()
    }

    override suspend fun handleCall(instanceId: Long, method: String, payload: ByteArray): ByteArray {
        val backend = instances[instanceId]
            ?: error("AdMob: unknown instance $instanceId")
        return when (method) {
            "load_interstitial" -> {
                try {
                    var cursor = 0
                    val d_ad_unit_id = Bincode.readString(payload, cursor); cursor = d_ad_unit_id.consumed
                    val ad_unit_id = d_ad_unit_id.value
                    val result = backend.load_interstitial(ad_unit_id)
                    val out = ByteArrayOutputStream()
                    codecs.writeNativeHandleId(out, result)
                    out.toByteArray()
                } catch (e: BackendException<*>) {
                    val err = e.error as? AdError ?: throw e
                    val errOut = ByteArrayOutputStream()
                    codecs.writeAdError(errOut, err)
                    throw PluginException(errOut.toByteArray())
                }
            }
            "show_interstitial" -> {
                try {
                    var cursor = 0
                    val d_ad = codecs.readNativeHandleId(payload, cursor); cursor = d_ad.consumed
                    val ad = d_ad.value
                    val result = backend.show_interstitial(ad)
                    val out = ByteArrayOutputStream()
                    codecs.writeInterstitialOutcome(out, result)
                    out.toByteArray()
                } catch (e: BackendException<*>) {
                    val err = e.error as? AdError ?: throw e
                    val errOut = ByteArrayOutputStream()
                    codecs.writeAdError(errOut, err)
                    throw PluginException(errOut.toByteArray())
                }
            }
            "load_rewarded" -> {
                try {
                    var cursor = 0
                    val d_ad_unit_id = Bincode.readString(payload, cursor); cursor = d_ad_unit_id.consumed
                    val ad_unit_id = d_ad_unit_id.value
                    val result = backend.load_rewarded(ad_unit_id)
                    val out = ByteArrayOutputStream()
                    codecs.writeNativeHandleId(out, result)
                    out.toByteArray()
                } catch (e: BackendException<*>) {
                    val err = e.error as? AdError ?: throw e
                    val errOut = ByteArrayOutputStream()
                    codecs.writeAdError(errOut, err)
                    throw PluginException(errOut.toByteArray())
                }
            }
            "show_rewarded" -> {
                try {
                    var cursor = 0
                    val d_ad = codecs.readNativeHandleId(payload, cursor); cursor = d_ad.consumed
                    val ad = d_ad.value
                    val result = backend.show_rewarded(ad)
                    val out = ByteArrayOutputStream()
                    codecs.writeRewardedOutcome(out, result)
                    out.toByteArray()
                } catch (e: BackendException<*>) {
                    val err = e.error as? AdError ?: throw e
                    val errOut = ByteArrayOutputStream()
                    codecs.writeAdError(errOut, err)
                    throw PluginException(errOut.toByteArray())
                }
            }
            "show_banner" -> {
                try {
                    var cursor = 0
                    val d_request = codecs.readBannerRequest(payload, cursor); cursor = d_request.consumed
                    val request = d_request.value
                    val result = backend.show_banner(request)
                    val out = ByteArrayOutputStream()
                    codecs.writeNativeHandleId(out, result)
                    out.toByteArray()
                } catch (e: BackendException<*>) {
                    val err = e.error as? AdError ?: throw e
                    val errOut = ByteArrayOutputStream()
                    codecs.writeAdError(errOut, err)
                    throw PluginException(errOut.toByteArray())
                }
            }
            "update_banner" -> {
                try {
                    var cursor = 0
                    val d_banner = codecs.readNativeHandleId(payload, cursor); cursor = d_banner.consumed
                    val banner = d_banner.value
                    val d_rect = codecs.readBannerRect(payload, cursor); cursor = d_rect.consumed
                    val rect = d_rect.value
                    backend.update_banner(banner, rect)
                    ByteArray(0)
                } catch (e: BackendException<*>) {
                    val err = e.error as? AdError ?: throw e
                    val errOut = ByteArrayOutputStream()
                    codecs.writeAdError(errOut, err)
                    throw PluginException(errOut.toByteArray())
                }
            }
            "hide_banner" -> {
                try {
                    var cursor = 0
                    val d_banner = codecs.readNativeHandleId(payload, cursor); cursor = d_banner.consumed
                    val banner = d_banner.value
                    backend.hide_banner(banner)
                    ByteArray(0)
                } catch (e: BackendException<*>) {
                    val err = e.error as? AdError ?: throw e
                    val errOut = ByteArrayOutputStream()
                    codecs.writeAdError(errOut, err)
                    throw PluginException(errOut.toByteArray())
                }
            }
            else -> error("unknown AdMob method: $method")
        }
    }
}
