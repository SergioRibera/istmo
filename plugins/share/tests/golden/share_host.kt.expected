// GENERATED by istmo-build - DO NOT EDIT.
// plugin id: istmo.share

import java.io.ByteArrayOutputStream

/// Platform-facing surface for the `Share` plugin. Hand-write an
/// impl with the SDK-specific logic; the dispatcher below handles
/// wire encode/decode.
interface ShareBackend {
     // Throw BackendException(ShareError) for domain errors.
    suspend fun share(request: ShareRequest): ShareOutcome

     // Throw BackendException(ShareError) for domain errors.
    suspend fun capabilities(): ShareCapabilities
}

/// Reader / writer per `Named` type referenced by the `Share` contract.
/// User provides one impl; each entry typically delegates to a
/// companion helper on the type itself.
interface ShareCodecs {
    fun readIncomingFile(bytes: ByteArray, offset: Int): Bincode.Decoded<IncomingFile>
    fun writeIncomingFile(out: ByteArrayOutputStream, value: IncomingFile)

    fun readIncomingShare(bytes: ByteArray, offset: Int): Bincode.Decoded<IncomingShare>
    fun writeIncomingShare(out: ByteArrayOutputStream, value: IncomingShare)

    fun readShareAnchor(bytes: ByteArray, offset: Int): Bincode.Decoded<ShareAnchor>
    fun writeShareAnchor(out: ByteArrayOutputStream, value: ShareAnchor)

    fun readShareCapabilities(bytes: ByteArray, offset: Int): Bincode.Decoded<ShareCapabilities>
    fun writeShareCapabilities(out: ByteArrayOutputStream, value: ShareCapabilities)

    fun readShareError(bytes: ByteArray, offset: Int): Bincode.Decoded<ShareError>
    fun writeShareError(out: ByteArrayOutputStream, value: ShareError)

    fun readShareFile(bytes: ByteArray, offset: Int): Bincode.Decoded<ShareFile>
    fun writeShareFile(out: ByteArrayOutputStream, value: ShareFile)

    fun readShareFileSource(bytes: ByteArray, offset: Int): Bincode.Decoded<ShareFileSource>
    fun writeShareFileSource(out: ByteArrayOutputStream, value: ShareFileSource)

    fun readShareOutcome(bytes: ByteArray, offset: Int): Bincode.Decoded<ShareOutcome>
    fun writeShareOutcome(out: ByteArrayOutputStream, value: ShareOutcome)

    fun readSharePreview(bytes: ByteArray, offset: Int): Bincode.Decoded<SharePreview>
    fun writeSharePreview(out: ByteArrayOutputStream, value: SharePreview)

    fun readShareRect(bytes: ByteArray, offset: Int): Bincode.Decoded<ShareRect>
    fun writeShareRect(out: ByteArrayOutputStream, value: ShareRect)

    fun readShareRequest(bytes: ByteArray, offset: Int): Bincode.Decoded<ShareRequest>
    fun writeShareRequest(out: ByteArrayOutputStream, value: ShareRequest)
}

/// Decodes wire calls, invokes `ShareBackend`, encodes the response.
/// Register with `IstmoRuntime.registerHandler(PLUGIN_ID, dispatcher)`.
class ShareDispatcher(
    private val backend: ShareBackend,
    private val codecs: ShareCodecs,
) : PluginHandler {

    companion object {
        const val PLUGIN_ID = "istmo.share"
    }

    override suspend fun handleCall(instanceId: Long, method: String, payload: ByteArray): PluginResult {
        return when (method) {
            "share" -> {
                try {
                    var cursor = 0
                    val d_request = codecs.readShareRequest(payload, cursor); cursor = d_request.consumed
                    val request = d_request.value
                    val result = backend.share(request)
                    val out = ByteArrayOutputStream()
                    codecs.writeShareOutcome(out, result)
                    PluginResult.Unary(out.toByteArray())
                } catch (e: BackendException) {
                    val err = e.error as? ShareError ?: throw e
                    val errOut = ByteArrayOutputStream()
                    codecs.writeShareError(errOut, err)
                    throw PluginException(errOut.toByteArray())
                }
            }
            "capabilities" -> {
                try {
                    val result = backend.capabilities()
                    val out = ByteArrayOutputStream()
                    codecs.writeShareCapabilities(out, result)
                    PluginResult.Unary(out.toByteArray())
                } catch (e: BackendException) {
                    val err = e.error as? ShareError ?: throw e
                    val errOut = ByteArrayOutputStream()
                    codecs.writeShareError(errOut, err)
                    throw PluginException(errOut.toByteArray())
                }
            }
            else -> error("unknown Share method: $method")
        }
    }
}
