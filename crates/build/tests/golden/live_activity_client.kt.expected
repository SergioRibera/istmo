// GENERATED by istmo-build - DO NOT EDIT.
// plugin id: istmo.live_activity

import java.io.ByteArrayOutputStream

/// Rust-hosted `LiveActivity` plugin. Ships each call across the frame
/// protocol via `IstmoRuntime.call(...)`.
class LiveActivityClient(
    private val codecs: LiveActivityCodecs,
) {

    companion object {
        const val PLUGIN_ID = "istmo.live_activity"
    }

    suspend fun start(activity_type: String, attributes: ByteArray, initial_state: ByteArray, style: ActivityStyle, stale_after_seconds: UInt?, android_tier_hint: AndroidTierHint?): NativeHandleId {
        val payloadBuf = ByteArrayOutputStream()
        Bincode.writeString(payloadBuf, activity_type)
        Bincode.writeBytes(payloadBuf, attributes)
        Bincode.writeBytes(payloadBuf, initial_state)
        codecs.writeActivityStyle(payloadBuf, style)
        Bincode.writeOption(payloadBuf, stale_after_seconds) { s, v -> Bincode.writeVarintU64(s, v.toLong()) }
        Bincode.writeOption(payloadBuf, android_tier_hint) { s, v -> codecs.writeAndroidTierHint(s, v) }
        val payload = payloadBuf.toByteArray()
        val bytes = try {
            IstmoRuntime.call(PLUGIN_ID, "start", payload)
        } catch (e: PluginException) {
            val decoded = runCatching { codecs.readActivityError(e.payload, 0).value }.getOrNull()
            throw LiveActivityException(e.payload, decoded)
        }
        var cursor = 0
        val d_result = codecs.readNativeHandleId(bytes, cursor); cursor = d_result.consumed
        val result = d_result.value
        return result
    }

    suspend fun update(handle: NativeHandleId, state: ByteArray, alert: AlertConfig?) {
        val payloadBuf = ByteArrayOutputStream()
        codecs.writeNativeHandleId(payloadBuf, handle)
        Bincode.writeBytes(payloadBuf, state)
        Bincode.writeOption(payloadBuf, alert) { s, v -> codecs.writeAlertConfig(s, v) }
        val payload = payloadBuf.toByteArray()
        try {
            IstmoRuntime.call(PLUGIN_ID, "update", payload)
        } catch (e: PluginException) {
            val decoded = runCatching { codecs.readActivityError(e.payload, 0).value }.getOrNull()
            throw LiveActivityException(e.payload, decoded)
        }
    }

    suspend fun end(handle: NativeHandleId, final_state: ByteArray?, dismissal: DismissalPolicy) {
        val payloadBuf = ByteArrayOutputStream()
        codecs.writeNativeHandleId(payloadBuf, handle)
        Bincode.writeOption(payloadBuf, final_state) { s, v -> Bincode.writeBytes(s, v) }
        codecs.writeDismissalPolicy(payloadBuf, dismissal)
        val payload = payloadBuf.toByteArray()
        try {
            IstmoRuntime.call(PLUGIN_ID, "end", payload)
        } catch (e: PluginException) {
            val decoded = runCatching { codecs.readActivityError(e.payload, 0).value }.getOrNull()
            throw LiveActivityException(e.payload, decoded)
        }
    }

    suspend fun are_activities_enabled(): Boolean {
        val payload = ByteArray(0)
        val bytes = try {
            IstmoRuntime.call(PLUGIN_ID, "are_activities_enabled", payload)
        } catch (e: PluginException) {
            val decoded = runCatching { codecs.readActivityError(e.payload, 0).value }.getOrNull()
            throw LiveActivityException(e.payload, decoded)
        }
        var cursor = 0
        val d_result = Bincode.readBool(bytes, cursor); cursor = d_result.consumed
        val result = d_result.value
        return result
    }

    suspend fun capabilities(): PlatformCapabilities {
        val payload = ByteArray(0)
        val bytes = try {
            IstmoRuntime.call(PLUGIN_ID, "capabilities", payload)
        } catch (e: PluginException) {
            val decoded = runCatching { codecs.readActivityError(e.payload, 0).value }.getOrNull()
            throw LiveActivityException(e.payload, decoded)
        }
        var cursor = 0
        val d_result = codecs.readPlatformCapabilities(bytes, cursor); cursor = d_result.consumed
        val result = d_result.value
        return result
    }

    suspend fun restore_active(): List<RestoredActivity> {
        val payload = ByteArray(0)
        val bytes = try {
            IstmoRuntime.call(PLUGIN_ID, "restore_active", payload)
        } catch (e: PluginException) {
            val decoded = runCatching { codecs.readActivityError(e.payload, 0).value }.getOrNull()
            throw LiveActivityException(e.payload, decoded)
        }
        var cursor = 0
        val d_result = Bincode.readVec(bytes, cursor) { p, o -> codecs.readRestoredActivity(p, o) }; cursor = d_result.consumed
        val result = d_result.value
        return result
    }
}

/// Kotlin-side view of a domain error decoded from `Frame::Respond { Err(bytes) }`.
class LiveActivityException(
    val payload: ByteArray,
    val decoded: ActivityError?,
) : RuntimeException()
