// GENERATED by istmo-build - DO NOT EDIT.
// plugin id: istmo.live_activity

import Foundation
import IstmoRuntime

public final class LiveActivityCodecsImpl: LiveActivityCodecs {
    public init() {}

    public func readActivityStyle(_ c: inout Bincode.Cursor) throws -> ActivityStyle {
        let disc = try Bincode.readVarintU32(&c)
        guard let value = ActivityStyle(rawValue: disc) else {
            throw Bincode.DecodeError.invalidTag(UInt8(clamping: disc))
        }
        return value
    }

    public func writeActivityStyle(_ out: inout Data, _ value: ActivityStyle) {
        Bincode.writeVarintU32(&out, value.rawValue)
    }

    public func readAlertSound(_ c: inout Bincode.Cursor) throws -> AlertSound {
        let disc = try Bincode.readVarintU32(&c)
        switch disc {
        case 0: return .`default`
        case 1: return .named(try Bincode.readString(&c))
        case 2: return .none
        default: throw Bincode.DecodeError.invalidTag(UInt8(clamping: disc))
        }
    }

    public func writeAlertSound(_ out: inout Data, _ value: AlertSound) {
        switch value {
        case .`default`:
            Bincode.writeVarintU32(&out, 0)
        case .named(let payload):
            Bincode.writeVarintU32(&out, 1)
            Bincode.writeString(&out, payload)
        case .none:
            Bincode.writeVarintU32(&out, 2)
        }
    }

    public func readAlertConfig(_ c: inout Bincode.Cursor) throws -> AlertConfig {
        let title = try Bincode.readString(&c)
        let body = try Bincode.readString(&c)
        let sound = try readAlertSound(&c)
        return AlertConfig(title: title, body: body, sound: sound)
    }

    public func writeAlertConfig(_ out: inout Data, _ value: AlertConfig) {
        Bincode.writeString(&out, value.title)
        Bincode.writeString(&out, value.body)
        writeAlertSound(&out, value.sound)
    }

    public func readDismissalPolicy(_ c: inout Bincode.Cursor) throws -> DismissalPolicy {
        let disc = try Bincode.readVarintU32(&c)
        switch disc {
        case 0: return .immediate
        case 1: return .`default`
        case 2: return .afterSeconds(try Bincode.readVarintU32(&c))
        default: throw Bincode.DecodeError.invalidTag(UInt8(clamping: disc))
        }
    }

    public func writeDismissalPolicy(_ out: inout Data, _ value: DismissalPolicy) {
        switch value {
        case .immediate:
            Bincode.writeVarintU32(&out, 0)
        case .`default`:
            Bincode.writeVarintU32(&out, 1)
        case .afterSeconds(let payload):
            Bincode.writeVarintU32(&out, 2)
            Bincode.writeVarintU32(&out, UInt32(payload))
        }
    }

    public func readAndroidTierHint(_ c: inout Bincode.Cursor) throws -> AndroidTierHint {
        let disc = try Bincode.readVarintU32(&c)
        guard let value = AndroidTierHint(rawValue: disc) else {
            throw Bincode.DecodeError.invalidTag(UInt8(clamping: disc))
        }
        return value
    }

    public func writeAndroidTierHint(_ out: inout Data, _ value: AndroidTierHint) {
        Bincode.writeVarintU32(&out, value.rawValue)
    }

    public func readIosCapabilities(_ c: inout Bincode.Cursor) throws -> IosCapabilities {
        let activity_kit_available = try Bincode.readBool(&c)
        let activities_enabled = try Bincode.readBool(&c)
        let dynamic_island = try Bincode.readBool(&c)
        let push_updates = try Bincode.readBool(&c)
        return IosCapabilities(activity_kit_available: activity_kit_available, activities_enabled: activities_enabled, dynamic_island: dynamic_island, push_updates: push_updates)
    }

    public func writeIosCapabilities(_ out: inout Data, _ value: IosCapabilities) {
        Bincode.writeBool(&out, value.activity_kit_available)
        Bincode.writeBool(&out, value.activities_enabled)
        Bincode.writeBool(&out, value.dynamic_island)
        Bincode.writeBool(&out, value.push_updates)
    }

    public func readAndroidCapabilities(_ c: inout Bincode.Cursor) throws -> AndroidCapabilities {
        let supports_custom = try Bincode.readBool(&c)
        let supports_progress_style = try Bincode.readBool(&c)
        let supports_live_update = try Bincode.readBool(&c)
        let notifications_enabled = try Bincode.readBool(&c)
        return AndroidCapabilities(supports_custom: supports_custom, supports_progress_style: supports_progress_style, supports_live_update: supports_live_update, notifications_enabled: notifications_enabled)
    }

    public func writeAndroidCapabilities(_ out: inout Data, _ value: AndroidCapabilities) {
        Bincode.writeBool(&out, value.supports_custom)
        Bincode.writeBool(&out, value.supports_progress_style)
        Bincode.writeBool(&out, value.supports_live_update)
        Bincode.writeBool(&out, value.notifications_enabled)
    }

    public func readPlatformCapabilities(_ c: inout Bincode.Cursor) throws -> PlatformCapabilities {
        let disc = try Bincode.readVarintU32(&c)
        switch disc {
        case 0: return .ios(try readIosCapabilities(&c))
        case 1: return .android(try readAndroidCapabilities(&c))
        case 2: return .unsupported
        default: throw Bincode.DecodeError.invalidTag(UInt8(clamping: disc))
        }
    }

    public func writePlatformCapabilities(_ out: inout Data, _ value: PlatformCapabilities) {
        switch value {
        case .ios(let payload):
            Bincode.writeVarintU32(&out, 0)
            writeIosCapabilities(&out, payload)
        case .android(let payload):
            Bincode.writeVarintU32(&out, 1)
            writeAndroidCapabilities(&out, payload)
        case .unsupported:
            Bincode.writeVarintU32(&out, 2)
        }
    }

    public func readRestoredActivity(_ c: inout Bincode.Cursor) throws -> RestoredActivity {
        let handle = try readNativeHandleId(&c)
        let activity_type = try Bincode.readString(&c)
        let attributes = try Bincode.readData(&c)
        let state = try Bincode.readData(&c)
        return RestoredActivity(handle: handle, activity_type: activity_type, attributes: attributes, state: state)
    }

    public func writeRestoredActivity(_ out: inout Data, _ value: RestoredActivity) {
        writeNativeHandleId(&out, value.handle)
        Bincode.writeString(&out, value.activity_type)
        Bincode.writeData(&out, value.attributes)
        Bincode.writeData(&out, value.state)
    }

    public func readActivityError(_ c: inout Bincode.Cursor) throws -> ActivityError {
        let disc = try Bincode.readVarintU32(&c)
        switch disc {
        case 0: return .notSupported
        case 1: return .disabled
        case 2: return .exceededMaximum
        case 3: return .handleNotFound
        case 4: return .unknownActivityType(try Bincode.readString(&c))
        case 5: return .decode(try Bincode.readString(&c))
        case 6: return .backend(try Bincode.readString(&c))
        default: throw Bincode.DecodeError.invalidTag(UInt8(clamping: disc))
        }
    }

    public func writeActivityError(_ out: inout Data, _ value: ActivityError) {
        switch value {
        case .notSupported:
            Bincode.writeVarintU32(&out, 0)
        case .disabled:
            Bincode.writeVarintU32(&out, 1)
        case .exceededMaximum:
            Bincode.writeVarintU32(&out, 2)
        case .handleNotFound:
            Bincode.writeVarintU32(&out, 3)
        case .unknownActivityType(let payload):
            Bincode.writeVarintU32(&out, 4)
            Bincode.writeString(&out, payload)
        case .decode(let payload):
            Bincode.writeVarintU32(&out, 5)
            Bincode.writeString(&out, payload)
        case .backend(let payload):
            Bincode.writeVarintU32(&out, 6)
            Bincode.writeString(&out, payload)
        }
    }

    public func readNativeHandleId(_ c: inout Bincode.Cursor) throws -> NativeHandleId {
        return try Bincode.readVarintU64(&c)
    }

    public func writeNativeHandleId(_ out: inout Data, _ value: NativeHandleId) {
        Bincode.writeVarintU64(&out, value)
    }
}
