[package]
name = "istmo-plugins-schema"
version = "0.1.0"
description = "Contract builders for the bundled istmo plugins — build-time only, consumed by istmo-plugins' build.rs and downstream apps that codegen native dispatchers."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
readme.workspace = true
keywords = ["istmo", "schema", "contract", "build-script", "codegen"]
categories = ["development-tools::build-utils"]

[dependencies]
istmo-build = { workspace = true }

[lints]
workspace = true
