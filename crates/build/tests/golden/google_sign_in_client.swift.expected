// GENERATED by istmo-build - DO NOT EDIT.
// plugin id: istmo.google_sign_in

import Foundation

/// Rust-hosted `SignIn` plugin. Ships each call across the frame
/// protocol via `IstmoRuntime.shared.call(...)`.
public final class SignInClient: SignIn {
    public static let PLUGIN_ID: String = "istmo.google_sign_in"

    public init() {}

    public func sign_in(mode: SignInMode) async throws -> SignInAccount {
        var payload = Data()
        Bincode.encode(&payload, mode)
        let bytes = try await IstmoRuntime.shared.call(pluginId: Self.PLUGIN_ID, method: "sign_in", payload: payload)
        return try Bincode.decode(bytes) as SignInAccount
    }

    public func silent_sign_in() async throws -> SignInAccount? {
        let payload = Data()
        let bytes = try await IstmoRuntime.shared.call(pluginId: Self.PLUGIN_ID, method: "silent_sign_in", payload: payload)
        return try Bincode.decode(bytes) as SignInAccount?
    }

    public func refresh(credential: NativeHandleId) async throws -> SignInAccount {
        var payload = Data()
        Bincode.encode(&payload, credential)
        let bytes = try await IstmoRuntime.shared.call(pluginId: Self.PLUGIN_ID, method: "refresh", payload: payload)
        return try Bincode.decode(bytes) as SignInAccount
    }

    public func sign_out() async throws -> Void {
        let payload = Data()
        let bytes = try await IstmoRuntime.shared.call(pluginId: Self.PLUGIN_ID, method: "sign_out", payload: payload)
        return try Bincode.decode(bytes) as Void
    }

    public func revoke() async throws -> Void {
        let payload = Data()
        let bytes = try await IstmoRuntime.shared.call(pluginId: Self.PLUGIN_ID, method: "revoke", payload: payload)
        return try Bincode.decode(bytes) as Void
    }
}

/// Swift-side view of a domain error decoded from `Frame::Respond { Err(bytes) }`.
public struct SignInException: Error {
    public let payload: Data
    public let decoded: SignInError?
}
